export default interface IAddress {
  address: string;
  zipCode: string;
  number: string;
  complemente: string;
  district: string;
  city: string;
  uf: string;
  // eslint-disable-next-line semi
}
